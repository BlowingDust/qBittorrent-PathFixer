# qBittorrent PathFixer 需求文档

## 1. 文档信息

- 项目名称：qBittorrent PathFixer
- 运行平台：Windows
- 开发语言：Python
- 目标 qBittorrent 版本：v4.6.7（64-bit）
- qBittorrent 运行平台：Linux 服务器
- WebUI 地址：由配置文件提供（示例：`http://qbittorrent.example.test:8080/`）
- 目标种子 v1 信息哈希：由配置文件提供
- 文档状态：需求确认稿

> WebUI 密码等敏感信息不得写入本文件、重命名记录或日志。

## 2. 项目背景

目标种子包含数千个文件，其中可能有数百个文件或目录因名称过长、路径过长，导致 Linux 无法创建文件，或在后续复制到 Windows 时出现兼容性问题。

需要开发一个在 Windows 中运行的 Python 程序，通过 qBittorrent WebUI API 获取目标种子的文件清单，检测名称及路径问题，生成可由用户逐项人工编辑的重命名记录。在用户完成编辑并确认无误后，程序才调用 qBittorrent API 执行重命名。

本工具不自动决定最终名称。自动生成名称只可作为未来的可选功能，不属于首个版本的必需范围。

## 3. 项目目标

1. 连接远程 qBittorrent WebUI 并验证服务及 API 兼容性。
2. 获取指定种子的完整文件清单。
3. 同时统计文件名、目录名和路径的字符数及 UTF-8 字节数。
4. 根据用户配置的限制识别需要处理的项目。
5. 检测会妨碍复制到 Windows 的名称和路径问题。
6. 导出持久化的重命名记录，供用户逐项填写新名称或新路径。
7. 在执行前对整份记录进行完整校验并生成预览。
8. 仅在用户明确确认后调用 API 执行重命名。
9. 保存执行结果和错误信息，以便审计、重试和人工处理。

## 4. 不在首个版本范围内的事项

- 自动缩短或智能生成最终文件名。
- 修改文件内容。
- 移动种子的保存位置。
- 操作目标种子以外的种子。
- 直接访问或修改 Linux 服务器上的文件系统。
- 绕过 qBittorrent 直接重命名已下载文件。
- 提供桌面图形界面或 Web 界面；首个版本采用命令行界面。
- 自动处理多个种子。

## 5. 用户角色

工具仅考虑一个本地操作者。操作者拥有 qBittorrent WebUI 的登录凭据以及目标种子的管理权限。

## 6. 核心工作流程

### 6.1 初始化配置

用户创建本地配置，至少包含：

- WebUI 基础地址；
- WebUI 用户名（可选）；
- WebUI 密码的安全输入方式（可选）；
- 目标种子信息哈希；
- 各项长度限制；
- 当前种子的 Windows 目标根目录；不同种子可以使用不同根目录，同一种子内所有文件使用同一个根目录；
- 网络连接及请求超时；
- 工作文件和日志输出目录。

程序不得把密码明文保存到需要提交或分享的配置文件中。用户名和密码均允许不配置，以支持 WebUI IP 白名单免认证场景。配置了认证信息时，首个版本至少支持通过环境变量或运行时密码提示输入密码。

### 6.2 连接检查

程序连接 WebUI；配置了认证信息时先登录，未配置时直接访问 API，并读取：

- qBittorrent 应用版本；
- Web API 版本；
- 目标种子的基本信息；
- 目标种子的文件列表。

如果需要认证但登录失败、免认证访问被拒绝、服务版本不兼容或目标种子不存在，程序必须停止，不得继续执行改名。

### 6.3 扫描

程序对种子内每个文件的相对路径进行分析，至少计算：

- 完整相对路径；
- 文件名；
- 扩展名；
- 每一级目录名；
- 文件名字符数；
- 文件名 UTF-8 字节数；
- 每一级目录名字符数；
- 每一级目录名 UTF-8 字节数；
- 完整相对路径字符数；
- 完整相对路径 UTF-8 字节数；
- 如果配置了 Windows 目标根目录，最终 Windows 绝对路径字符数。

字符数使用 Python Unicode 字符串长度进行统计；字节数使用 UTF-8 编码后的长度进行统计。报告中应同时保留两者，不得相互替代。

### 6.4 问题识别

长度阈值必须由用户配置，且下列限制应能够分别设置或禁用：

- 文件名最大字符数；
- 文件名最大 UTF-8 字节数；
- 目录名最大字符数；
- 目录名最大 UTF-8 字节数；
- 完整相对路径最大字符数；
- 完整相对路径最大 UTF-8 字节数；
- Windows 最终绝对路径最大字符数。

程序还应检测下列 Windows 兼容性问题：

- 文件名或目录名包含 `< > : " / \\ | ? *`；
- 名称以空格或句点结尾；
- 名称为 Windows 保留设备名，例如 `CON`、`PRN`、`AUX`、`NUL`、`COM1` 至 `COM9`、`LPT1` 至 `LPT9`，并考虑带扩展名的形式；
- 名称为空、为 `.` 或为 `..`；
- 重命名后发生路径重复；
- 在 Windows 不区分大小写的语义下发生路径冲突。

每条问题必须提供机器可读的问题代码和便于人工理解的说明。一个文件可以同时包含多个问题。

### 6.5 导出人工编辑记录

扫描结果应导出为 UTF-8 编码的 CSV 文件，便于使用 Excel 或文本编辑器逐项修改。程序还应保存一份 JSON 快照，作为不可编辑的原始扫描基线和后续一致性校验依据。

CSV 至少包含以下字段：

| 字段 | 含义 | 用户是否编辑 |
|---|---|---|
| `record_id` | 稳定的记录编号 | 否 |
| `torrent_hash` | 种子信息哈希 | 否 |
| `item_type` | `file` 或 `folder` | 否 |
| `file_index` | qBittorrent 文件 index；文件夹为空，用于区分重复内部路径 | 否 |
| `old_path` | 当前种子内相对路径 | 否 |
| `new_path` | 用户填写的新相对路径 | 是 |
| `issues` | 检测到的问题代码 | 否 |
| `old_name_chars` | 当前名称字符数 | 否 |
| `old_name_bytes` | 当前名称 UTF-8 字节数 | 否 |
| `old_path_chars` | 当前路径字符数 | 否 |
| `old_path_bytes` | 当前路径 UTF-8 字节数 | 否 |
| `action` | `rename`、`skip` 或空 | 是 |
| `note` | 用户备注 | 是 |

为了避免 Excel 错误识别编码，CSV 应使用 UTF-8 with BOM。路径在 API、JSON 和 CSV 中统一使用 `/` 作为种子内部路径分隔符。

只导出存在问题的文件和目录。没有检测到问题的项目不得列入人工编辑 CSV，但仍保留在 JSON 原始快照中，供冲突校验使用。

### 6.6 人工编辑

用户逐项填写 `new_path` 并将 `action` 设置为 `rename`。`new_path` 可以同时改变目录结构和末级文件名。工具不得擅自填写或覆盖用户已经填写的新路径。

用户可以用 `skip` 表示已审阅但暂不处理。空 `action` 表示尚未决定。

### 6.7 执行前校验

执行前必须重新读取 qBittorrent 当前文件清单，并完成以下检查：

1. 信息哈希与计划一致。
2. 所有 `old_path` 仍然存在。
3. `new_path` 非空且是相对路径。
4. `new_path` 不包含路径穿越片段，例如 `..`。
5. 新名称及路径满足当前配置的全部限制。
6. 新路径满足 Windows 名称兼容规则。
7. 不同记录不会生成相同的新路径。
8. 新路径不会与未改名的现有路径冲突。
9. 在 Windows 不区分大小写的语义下不存在冲突。
10. 文件与目录之间不存在结构冲突。
11. 计划不存在重复操作、循环依赖或顺序导致的临时名称冲突。
12. 原始扫描快照与当前种子结构不存在影响本次操作的漂移。
13. 文件路径变更与文件夹路径变更组合后，所有受影响文件的最终路径唯一且与用户计划一致。

只要存在校验错误，整批操作默认不得开始。程序应输出校验报告供用户修正。

### 6.8 执行确认

执行前程序必须显示摘要，包括：

- 目标 WebUI 地址；
- qBittorrent 版本和 API 版本；
- 目标种子名称及哈希；
- 文件改名数量；
- 目录改名数量；
- 跳过数量；
- 校验警告数量；
- 执行记录文件位置。

用户必须输入包含目标种子识别信息的明确确认内容后才能执行。仅使用普通的 `y` 不足以防止误操作。程序还应支持 `--dry-run`，且默认行为必须等同于预演，不执行写操作。

### 6.9 执行重命名

qBittorrent v4.6.7 支持基于路径的 Web API：

- `POST /api/v2/torrents/renameFile`
- `POST /api/v2/torrents/renameFolder`

请求使用 `hash`、`oldPath`、`newPath` 参数。实际开发时应通过 Web API 版本检查确认接口兼容性，不应只依赖应用版本字符串。

执行要求：

- 逐项执行并记录结果；
- qBittorrent 的重命名在底层异步完成；HTTP 2xx 仅代表请求已受理，不代表磁盘和内部路径已经完成变更；
- 每次写请求后必须轮询 `torrents/files`，确认该操作影响的全部文件路径已达到预期状态，再发送下一项请求；轮询间隔、单项超时和确认后的稳定间隔应可配置；
- 每次请求设置合理超时；
- 对网络错误使用有限次数重试；
- 对 HTTP 409 等业务错误不得盲目重试；
- 文件夹重命名与文件重命名必须按经过规划的安全顺序执行；不得假定扫描时的 `old_path` 在前置操作后仍然有效；
- 执行器必须维护“原始路径—当前路径—最终路径”的运行时映射。每次文件夹操作成功后，应立即更新其所有后代项目的当前路径，后续 API 请求只能使用更新后的当前路径；
- 对一个项目同时修改目录结构和文件名时，规划器应将用户填写的最终 `new_path` 编译为有序的文件与文件夹 API 操作，而不是按 CSV 行顺序直接发送请求；
- 如果若干文件共享同一个目录变更，应在不改变用户最终结果的前提下优先合并为一次 `renameFolder`；文件特有的名称或目录变更使用 `renameFile`；
- 每次操作前可对照内存中的当前路径映射，并在必要时通过只读 API 复核路径存在性；
- 如存在路径互换、大小写修改或目标暂时占用，应使用确定且唯一的临时路径分阶段完成；
- 任意单项发生请求失败、业务错误或结果无法确认时，立即停止整批操作，不再发送任何后续重命名请求；
- 不承诺自动回滚已经成功的操作，但必须生成可供人工恢复的反向映射记录。

### 6.10 执行后验证

执行结束后重新获取种子文件列表，并确认：

- 每个成功项目的新路径已经存在；
- 旧路径已经消失；
- 失败项目仍有明确状态；
- 当前结构不存在新增冲突；
- 实际状态与执行日志一致。

最终输出一份执行报告，状态至少包括：`pending`、`validated`、`success`、`failed`、`skipped`、`not_run`。

## 7. 命令行功能建议

首个版本建议提供以下命令：

```text
python -m qbittorrent_pathfixer connection-test
python -m qbittorrent_pathfixer scan
python -m qbittorrent_pathfixer validate --plan <CSV路径>
python -m qbittorrent_pathfixer apply --plan <CSV路径> --dry-run
python -m qbittorrent_pathfixer apply --plan <CSV路径>
python -m qbittorrent_pathfixer verify --run <执行记录路径>
```

具体参数名称可在技术设计阶段调整，但必须保持扫描、校验、执行和验证相互分离。

## 8. 配置需求

建议使用 TOML 配置文件，示意如下：

```toml
[qbittorrent]
base_url = "http://qbittorrent.example.test:8080"
# username = "admin"  # 可选；免认证时不配置
torrent_hash = "0123456789abcdef0123456789abcdef01234567"
timeout_seconds = 15

[limits]
filename_chars = 200
filename_utf8_bytes = 220
directory_chars = 200
directory_utf8_bytes = 220
relative_path_chars = 1000
relative_path_utf8_bytes = 3800
windows_absolute_path_chars = 240

[windows]
destination_root = "D:/Downloads"

[output]
directory = "work"
```

以上数值作为首个版本的建议默认值，均有意留出安全余量，不贴近 Linux 单个名称通常为 255 字节、路径通常为 4096 字节以及传统 Windows `MAX_PATH` 260 字符的边界。每项限制应允许通过配置显式关闭。

Windows 最终绝对路径长度应使用当前种子的 `destination_root`、路径分隔符和文件相对路径动态计算；不同种子可以配置不同的 `destination_root`。`windows_absolute_path_chars = 240` 是带安全余量的默认预警/阻断阈值，而不是声称 Windows 在所有配置下都具有相同硬限制。

`username` 和密码均为可选项。未配置时程序不调用登录接口，直接测试 API 访问；只配置其中一项视为配置错误。配置密码时从专用环境变量或交互式提示读取。

## 9. 数据与日志

每次扫描和执行应创建独立、有时间戳的工作目录，保存：

- 原始 API 信息摘要；
- 原始文件列表 JSON 快照；
- 人工编辑用 CSV；
- 校验报告；
- 实际发送的操作清单；
- 每项操作的响应状态；
- 执行后文件列表快照；
- 正向和反向路径映射。

日志不得包含 WebUI 密码、会话 Cookie 或其他认证密钥。日志中的 URL 应避免拼接敏感查询参数。

## 10. 安全性与可靠性要求

- 默认不修改任何内容。
- 所有写操作都必须经过完整校验和显式确认。
- 认证只允许发送至配置的 WebUI 主机，不跟随跨主机重定向。
- 配置中的基础 URL 应固定协议、主机和端口。
- API 会话应在程序结束时注销。
- 所有输出文件采用稳定、可重复读取的格式。
- 重复运行校验或预演不得改变 qBittorrent 状态。
- 执行记录必须能够区分“请求未发送”“请求失败”和“请求成功但验证失败”。
- 程序异常退出后，可根据执行记录继续验证或处理尚未执行的项目。

## 11. 性能要求

- 支持单个种子至少 10,000 个文件。
- 扫描和本地校验应在普通 Windows 电脑上于合理时间内完成。
- API 重命名按受控速率逐项执行，优先保证可追踪性和正确性，不以最大吞吐量为目标。
- 大型文件列表的日志和报告不得依赖一次性打印到终端。

## 12. 兼容性要求

- 客户端运行环境：Windows 10/11。
- Python：建议支持 Python 3.11 至 3.14；最终支持范围以依赖库测试结果为准。
- 服务端：qBittorrent v4.6.7，Web API 版本在运行时检测。
- 路径内部表示统一为 Unicode 字符串和 `/` 分隔符。
- CSV 应能被常见版本的 Microsoft Excel 正确打开。

当前仓库中的 `venv` 是 Windows 虚拟环境，只用于本机开发，不应复制到服务器或纳入发布包。项目应使用 `pyproject.toml` 或锁定版本的依赖清单重建环境。

## 13. 验收标准

### 13.1 扫描

- 能成功登录指定 WebUI 并定位目标种子。
- 能获取数千个文件并生成完整快照。
- 能正确报告 ASCII、中文、组合 Unicode 字符的字符数和 UTF-8 字节数。
- 能分别根据各项可配置阈值筛选问题。
- 能识别规定的 Windows 非法名称及大小写冲突。

### 13.2 记录与人工编辑

- 能生成 Excel 可读取的 CSV。
- 用户修改并保存 CSV 后，程序能稳定读取中文路径。
- 程序不会覆盖用户填写的 `new_path`、`action` 和 `note`。
- 原始快照可以证明计划对应哪一次扫描。

### 13.3 校验

- 对空名称、绝对路径、路径穿越、超限、非法字符和重复目标给出明确错误。
- 对已被外部修改的 `old_path` 拒绝执行。
- 对 Windows 大小写冲突给出明确错误。
- 任何阻断错误存在时，默认不会发送重命名请求。

### 13.4 执行

- `--dry-run` 不发送任何重命名请求。
- 未完成明确确认时不发送请求。
- 测试种子上的文件和目录可以通过 API 正确改名。
- 每个请求均有可审计记录，且记录不泄漏认证信息。
- 任一单项失败后不再发送后续操作，并能准确标记已成功、失败和未执行项目。
- 执行后验证能够发现实际路径与计划不一致。

## 14. 测试要求

至少包含以下自动化测试：

- 字符数和 UTF-8 字节数统计；
- 多后缀、无后缀、点文件和 Unicode 文件名解析；
- Windows 非法字符及保留名称检测；
- 大小写不敏感的冲突检测；
- 文件、文件夹及路径长度限制；
- CSV 的导出和重新导入；
- 用户字段保留；
- 重命名计划的冲突和依赖排序；
- 文件夹改名后其后代文件当前路径的更新；
- 同时修改目录结构与文件名时的操作编译及顺序；
- API 登录、版本检测、获取文件、重命名和错误处理；
- 网络超时及中途失败；
- 执行后验证。

API 测试应优先使用模拟响应。真实写操作只能针对专用测试种子进行。

## 15. 开发阶段划分

### 阶段一：只读扫描器

- 建立项目骨架和配置加载；
- 实现 WebUI 登录、版本检测和连接测试；
- 获取目标种子及文件列表；
- 实现长度统计和 Windows 兼容性检测；
- 输出 JSON 快照、CSV 和扫描摘要。

### 阶段二：计划校验器

- 读取人工编辑后的 CSV；
- 实现路径规范化、限制检查和冲突检查；
- 检测种子结构漂移；
- 生成校验报告及 dry-run 摘要。

### 阶段三：安全执行器

- 实现文件及目录重命名 API；
- 实现操作排序、临时路径和有限重试；
- 实现明确确认机制；
- 实现逐项执行日志和中断恢复信息。

### 阶段四：执行后验证与完善

- 实现执行后 API 复查；
- 生成正向及反向映射；
- 完善异常场景测试；
- 编写 Windows 安装与使用说明。

## 16. 已确认事项

1. Windows 目标根目录按种子配置；同一种子内所有文件共享同一目标根目录。
2. 长度限制采用带安全余量的默认值，并允许逐项配置或关闭。
3. 没有问题的文件不进入人工编辑 CSV，但必须进入原始 JSON 快照和冲突检查集合。
4. 用户可以同时修改目录结构和文件名。
5. 工具同时支持 `renameFile` 和 `renameFolder`，并负责正确规划混合操作顺序和路径更新。
6. 任意单项失败时立即停止整批操作。
7. WebUI 可以使用 HTTP；具体地址由配置文件提供。
8. WebUI 可能通过 IP 白名单允许免认证访问，因此用户名和密码为成对出现的可选配置。
9. 当前未发现额外的 CSRF、Host header 或反向代理限制；连接测试阶段仍应给出实际错误信息。

## 17. 已确认的人工记录交互规则

1. CSV 中的文件记录和文件夹记录统一填写完整的最终相对路径，不另设需要用户维护的目录映射表。程序负责汇总记录、推导目录映射，并编译为有序的 `renameFile` 和 `renameFolder` 操作。
2. 如果某个问题文件夹及其后代问题文件均列入 CSV，两类记录都必须填写最终路径，且父子路径关系必须一致。校验器负责发现并阻止不一致的计划。
3. 没有问题的项目不列入 CSV。文件夹改名间接影响这些项目时，程序根据完整 JSON 快照自动计算其最终路径，并将其纳入路径长度、Windows 兼容性、大小写冲突和目标重复检查，不要求用户逐项填写。
4. CSV 是用户意图的输入记录，不是可直接按行执行的 API 请求清单。只有经过完整快照展开、父子关系校验、冲突检测和操作排序后生成的执行计划，才允许进入确认与执行阶段。
